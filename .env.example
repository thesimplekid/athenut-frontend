# PUBLIC_API_URL is read at compile time (option_env!).
# Export it before running trunk, e.g.:  PUBLIC_API_URL="https://athenut.com" trunk serve
# Leave empty for relative paths (frontend and backend on same domain)
PUBLIC_API_URL=""
